# Rust 应用开发约定

## 目标与适用范围

本项目的 Rust 代码面向掌握基础用法、但不熟悉复杂借用和异步机制的维护者。编写容易定位、顺着主流程读懂、能够安全修改的命令行工具和后端服务，不把应用默认设计成通用框架。

取舍顺序：正确性、安全性和明确的资源约束 → 当前需求 → 可读性与可维护性 → 有证据支持的性能优化 → 复用与扩展。中文说明和注释准确、充分，但不逐行翻译语法。

## 工作流入口

涉及 Rust 设计、实现、重构或代码审查时，先读取并采用 [rust-readable-apps](.agents/skills/rust-readable-apps/SKILL.md)。详细例子按需读取，不必预读整份参考文档。纯文案修改不必加载整个编码流程，但必须核对相关代码契约。技能文件缺失时说明情况，并按本文件执行，不假称已加载。

rust-readable-apps 是主工作流；[rust-best-practices](.agents/skills/rust-best-practices/SKILL.md) 保留为通用参考，不默认重复加载。涉及任务生命周期、取消、背压或阻塞边界时，按需查阅 [rust-async-patterns](.agents/skills/rust-async-patterns/SKILL.md) 的相关章节。项目协议、数据库、故障恢复及运行边界见 [Rust 开发约束](docs/rust-development.md)，不能因工作流统一而删除这些约束。

遵守适用的指令层级和用户当前任务。保留项目明确的协议、数据兼容、安全及运行约束；不能借“保持简单”绕过它们。历史文档与本文件出现风格冲突时，在任务范围内统一；涉及行为或数据兼容的冲突，先说明影响，不静默删除旧能力。不要声称普通文档能够覆盖更高优先级指令。

## 必须保持的代码风格

- **只实现当前需要的能力。** 默认选定一种清楚的策略。不自动增加环境变量、配置优先级、CLI 开关、格式切换、插件接口、备用实现或预留 API。“以后可能需要”不是理由。秘密、用户输入和实际部署差异不能盲目硬编码；确有外部输入时仍需校验。
- **默认直接实现。** 优先具体类型、普通函数和普通控制流。允许少量清楚的重复，不仅为去重增加 trait、泛型、宏或转发层。框架要求、真实替换边界和明显降低复杂度的抽象可以保留。
- **职责与依赖清楚。** 按功能、状态所有权及变化原因组织模块。名称与实际职责一致；应用组装与底层存储不要职责混杂。发现双向模块依赖时检查原因，不仅移动文件或扩大 `pub` 范围。
- **按实际文件数量组织模块。** 单文件模块使用 `模块名.rs`，不创建仅含 `mod.rs` 的目录；模块需要两个及以上文件（包括入口和独立测试文件，可位于子目录）时，使用 `模块名/mod.rs` 作为目录入口。`mod.rs` 可以包含清楚的实现，不强制只放声明。crate/编译目标入口如 `main.rs`、`lib.rs`、`build.rs`、独立集成测试和示例入口按 Cargo 布局保留；小型内联测试模块可以保留。不要用 `#[path]` 或 `include!` 绕过约定，也不要手工改生成代码和第三方代码。
- **让主流程可读。** 防止单文件和单函数同时承担组装、调度、业务规则、I/O、重试和统计。按完整职责提取，不按行数机械拆碎。线性的日志初始化允许集中写；拆函数不等于新增任务、通道或共享状态。
- **名称表达真实行为。** 申请许可不叫建立连接，入队不叫已提交，取消通知不叫已完成。变量包含必要的业务含义和单位。避免缺少上下文的 `Manager`、`Processor`、`Context`、`data`、`tmp`；局部含义明显的短名称不必强行拉长。
- **控制表达密度。** 优先提前返回、`?`、`let … else` 和清楚的循环。配置用的 builder 链可以直接写；不把复杂状态修改塞进表达式链。SQL、宏体、复杂条件主动分行，避免多次状态更新挤在一行。
- **提供良好的中文注释。** 模块写明职责与入口；非简单类型、函数和关键字段说明业务含义、约束、返回值及错误条件。借用、克隆、锁、通道、后台任务和 guard 在容易误解的边界解释原因与释放时机。修改行为时同步更新注释和示例；不以注释比例验收，不用长注释为多余设计辩护。
- **保留必要的可靠性。** 不为少写代码删除校验、有界并发、事务、超时或退出收尾。先理清所有权和作用域，再决定是否需要 `clone`、`Arc` 或锁。新增任务必须有所有者、完成确认和错误处理；发送取消不等于任务已退出。

## 开始与完成

先查看相关入口、调用者、测试、`Cargo.toml`、工具链和现有检查方式；有 Git 时先检查工作区，保护用户未提交的修改。小改动直接处理；新功能或跨模块调整先简要说明职责与文件归属，不默认生成长篇设计文档或整套目录骨架。

旧项目按任务范围渐进整理：新模块遵守布局；相关模块的迁移与调用点、测试路径、文档链接同步处理。不要为一个小修复全仓搬文件。保持行为的重构与删除功能、变更配置、改变并发行为分开进行。

修改 Rust 代码后，优先执行项目已有检查；没有约定时从以下基线选取适用命令：

```sh
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets
```

先检查默认测试是否访问公网、依赖真实服务或修改持久数据。确认是安全的本地回归后再运行 `cargo test --workspace`；否则选择受影响的离线/loopback 测试，并明确未运行的范围。已有锁文件且无需更新依赖时，为支持的 Cargo 命令添加 `--locked`。新项目首次生成锁文件不强加 `--locked`。

沿用项目 feature 组合，不盲目使用 `--all-features`。不整组开启 `clippy::restriction`，不默认强开 `pedantic`；`-D warnings` 服从现有 CI 要求。不用全局 `allow`、删除测试或关闭校验换取通过。新增行为有相应测试；异步变化覆盖相关失败、取消和资源释放。

完成前检查 diff，沿关键入口重新阅读：功能在哪里、状态归谁、主要步骤是什么、失败或取消后怎样收尾。简要报告修改、实际验证和剩余风险。未运行的检查明确标注；只改文档或 skill 不宣称业务测试通过。不擅自提交、推送、发布或启动长时间公网任务。
